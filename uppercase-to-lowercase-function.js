// uppercase to lower case function toLower()

function convertToLowerCase(text) {
    var lowerCase = text.toLowerCase();
    return lowerCase;
}

var inputText = 'I AM SAIMA';
var outputText = convertToLowerCase(inputText);
console.log(outputText);